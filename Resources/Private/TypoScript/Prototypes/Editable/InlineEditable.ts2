prototype(NT:InlineEditable) < prototype(NT:Content) {
	@context.inlineTag = ${this.inlineTag}
	property = 'text'
	inlineTag = 'span'

	content = Fusion:Tag {
		tagName = ${inlineTag}
		content = ${q(node).property(property)}
		@process.contentElementEditable = ContentElementEditable
	}
}
